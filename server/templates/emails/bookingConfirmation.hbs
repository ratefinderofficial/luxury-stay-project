<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: 'Arial', sans-serif; background-color: #f3f4f6; }
        .container { max-width: 600px; margin: 20px auto; background-color: #ffffff; border-radius: 8px; overflow: hidden; }
        .header { background-color: #1e3a8a; color: #ffffff; padding: 20px; text-align: center; }
        .content { padding: 30px; color: #333; }
        .details-table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        .details-table td { padding: 12px; border-bottom: 1px solid #e5e7eb; }
        .details-table tr:last-child td { border-bottom: none; }
        .label { font-weight: bold; color: #555; }
        .highlight { color: #d97706; font-weight: bold; }
        .footer { background-color: #f9fafb; padding: 15px; text-align: center; font-size: 12px; color: #888; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Reservation Confirmed</h1>
        </div>
        <div class="content">
            <p>Dear {{name}},</p>
            <p>Thank you for choosing <strong>LuxuryStay</strong>. Your reservation has been confirmed. We look forward to hosting you!</p>
            
            <table class="details-table">
                <tr>
                    <td class="label">Booking ID:</td>
                    <td class="highlight">{{bookingId}}</td>
                </tr>
                <tr>
                    <td class="label">Room Type:</td>
                    <td>{{roomType}}</td>
                </tr>
                <tr>
                    <td class="label">Check-in:</td>
                    <td>{{checkIn}} (from 14:00)</td>
                </tr>
                <tr>
                    <td class="label">Check-out:</td>
                    <td>{{checkOut}} (by 11:00)</td>
                </tr>
                <tr>
                    <td class="label">Total Amount:</td>
                    <td class="highlight">${{totalAmount}}</td>
                </tr>
            </table>

            <p style="margin-top: 20px;">Please present this email or your ID upon arrival.</p>
        </div>
        <div class="footer">
            <p>Need to make changes? Contact us at reservations@luxurystay.com</p>
        </div>
    </div>
</body>
</html>